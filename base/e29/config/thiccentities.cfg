# Configuration file

better_diving {

    entity_peeper {
        D:armor=0.0
        D:damage=1.0
        D:followRange=1.0
        D:health=1.0
        D:jump=1.0
        D:scale=1.0
        D:speed=1.0
        D:toughness=0.0
    }

    peeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=3.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bladderfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=3.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    garryfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=3.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    holefish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=3.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    boomerang {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=3.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


cyclicmagic {

    robot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


deliverymerchants {

    merchant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


embers {

    ancient_golem {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


ftrsquids {

    rocketsquid {
        D:armor=0.0
        D:damage=1.0
        D:followRange=1.0
        D:health=1.0
        D:jump=1.0
        D:scale=1.0
        D:speed=1.0
        D:toughness=0.0
    }

    babyrocketsquid {
        D:armor=0.0
        D:damage=1.0
        D:followRange=1.0
        D:health=1.0
        D:jump=1.0
        D:scale=1.0
        D:speed=1.0
        D:toughness=0.0
    }

}


glacidus {

    drop_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    porcali {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.1

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    merialces {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    morprous {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frigispitor {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


jaff {

    cod {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=3.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    salmon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=3.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    clownfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=3.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pufferfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=3.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


minecraft {

    elder_guardian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wither_skeleton {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    stray {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    husk {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie_villager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton_horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie_horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    donkey {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mule {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    evocation_illager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vex {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vindication_illager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    illusion_illager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    giant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ghast {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie_pigman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    enderman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cave_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    silverfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    blaze {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    magma_cube {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wither {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    witch {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    endermite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    guardian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    shulker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pig {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sheep {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    chicken {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    squid {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wolf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mooshroom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    snowman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ocelot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    villager_golem {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rabbit {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    polar_bear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    llama {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    parrot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    villager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    player {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity's reach distance by. Default: 1
        D:reach=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


natura {

    imp {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    nitrocreeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    heatscarspider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    babyheatscarspider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


openblocks {

    luggage {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mini_me {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


rats {

    rat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    illager_piper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ratlantean_spirit {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    marbled_cheese_golem {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    feral_ratlantean {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    neo_ratlantean {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pirat_boat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pirat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    plague_doctor {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    black_death {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    plague_cloud {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    plague_beast {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


rocketsquidsft {

    rocketsquid {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    babyrocketsquid {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


tfa {

    tfa_crocodile {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=0.75

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_ostrich {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_piranha {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_penguin {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_shark {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=0.75

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_crocodile_dead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_crocodile_swimming {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=0.75

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_ostrich_dead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_penguin_dead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_penguin_swimming {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_piranha_dead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tfa_shark_dead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


thermalfoundation {

    blizz {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=0.755

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    blitz {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=0.755

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    basalz {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=0.755

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


treasure2 {

    wood_mimic {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


zawa {

    blackrhinoceros {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ball {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    treefrog {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    fijibandediguana {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    grevyszebra {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bottlenosedolphin {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    macaw {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pacificwalrus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hawksbillseaturtle {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hawksbillseaturtleegg {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    reticulatedgiraffe {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bluefish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    clownfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pufferfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sockeyesalmon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    orca {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cockatoo {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toucan {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    albatross {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    andeancondor {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    nilehippopotamus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    meerkat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    amurleopard {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    africanlion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    redpanda {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    polarbear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    westernlowlandgorilla {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    indianpangolin {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    indiangharial {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bengaltiger {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    asianelephant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    blackspidermonkey {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    gaur {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    japanesegiantsalamander {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    koala {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    japanesegiantsalamanderegg {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    okapi {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    redkangaroo {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    animaltransportvehicle {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    offroadvehicle {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    animaltrader {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    feedtrader {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zookeeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


